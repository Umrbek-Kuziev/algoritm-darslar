# Finding a number greater than three
Start
create a, b, c
give them value a, b, c
------------------------
  def greatestNum(a, b, c):
     if a>b:
       if a>c:
          return a
       else:
          return c
     else:
       if b>c:
          return b
       else:
          return c
